name: "Linux Clang"

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  BUILD_TYPE: Release
  CXX: clang-9

jobs:
  job:
    name: "Linux Clang"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
     
      - name: Configure
        run: mkdir build && cd build && cmake -DBUILD_ROOT_INTERFACE=OFF ..
      - name: Build
        run: cmake --build build
      - name: Install
        run: cmake --install .
